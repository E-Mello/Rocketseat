// Operador de String ( String operator)


// comparison (comparacao)
console.log('a' == 'a');

// concatenation (concaatenacao)
// Retorna a uniao de duas string
let alpha = 'alpha'
console.log(alpha += 'bet' + 's');